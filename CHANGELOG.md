# 更新日志

## \[2.0.0] - 2026-08-16

### 新增

- 支持升级到 `SeeDance-2.5` 视频生成模型，分镜视频生成、参考视频输入与最终合成链路统一适配 `MOV` 输出。
- 支持“并发”与“延长”双模式视频生成：
  - “并发”模式按视频生成并发数同时生成多个分镜，提升多分镜长视频整体产出速度。
  - “延长”模式串行生成分镜，并从分镜 2 开始引用上一分镜视频作为 `reference_video`，提升角色、服装、场景和光线的连续性。
- 支持连环画 PDF 生成：进入分镜视频阶段时并行导出以剧本名命名的 PDF，包含封面、角色页和逐分镜故事版内容，并上传到 `TOS` 供 Web UI 下载。
- 新增项目刷新恢复能力，通过 `sessionStorage` 与 `/project/{id}/restore` 恢复剧本、参考图、分镜、审核状态和连环画 PDF 下载状态。

### 优化

- 优化长视频分镜生成与审核的云端稳定性，后台长任务通过稳定 `client_id` 路由到最新 WebSocket 连接，降低 API 网关 504 或浏览器重连导致前端拿不到结果的风险。
- 优化并行分镜处理：单个分镜完成后即时持久化项目状态，避免序号靠后的分镜或审核结果在长任务中丢失。
- 优化视频审核失败后的自动流程：达到重试上限时可接受最高分结果继续合成，并标记为“已接受（超过重试次数）”。
- 优化线程池资源隔离，将交互式请求与生成任务分离，缓解多窗口并发生成时上传、状态恢复等请求被生成任务阻塞的问题。

### 修复

- 修复带图上传时 `auto` 全自动模式因草稿项目 ID 初始化顺序导致无法自动推进的问题。
- 修复重新生成分镜时的合成阻塞逻辑，确保存在未完成或未通过审核的重生成分镜时不会提前触发最终合成。
- 修复刷新页面后进度条和已完成步骤状态恢复不完整的问题。
- 修复结束项目时上游返回非 JSON 504 响应导致前端解析崩溃的问题。
- 修复云端连环画 PDF 中文、日文、韩文与 emoji 缺字乱码问题：PDF 生成强制使用 CJK 字体，运行时镜像需安装 `fonts-noto-cjk`、`fonts-noto-color-emoji`、`fontconfig` 以及 `ffmpeg` / `ffprobe`。

### 文档

- 更新中英文 README，说明 `SeeDance-2.5`、双视频生成模式、连环画 PDF、云端运行时字体与媒体依赖、凭证治理和部署要求。
- 更新多语言 i18n 文案，补充连环画 PDF 下载、生成中、生成完成和失败状态展示。

## \[1.1.0] - 2026-05-09

### 新增

- 新增应用版本号与静态资源版本号管理，统一定义在 `app/__init__.py`。
- 新增角色参考图库与布景参考图库的协同支持，纳入统一生成流程。
- 新增更完整的中英文版本说明文档与独立 `CHANGELOG` 文档。
- 新增示例截图与录屏文件在仓库文档中的展示入口。
- 新增针对 `ImageAgent` 多参考图映射能力的测试用例。

### 优化

- 优化分镜视频生成策略，提升长视频内容完整性与前后镜头衔接一致性。
- 优化配置治理方式，强调模型 `apikey`、`AK/SK`、`appid` 仅通过 `config.yaml` 读取。
- 优化前端静态资源缓存控制与版本管理，便于发布后快速生效。
- 优化多语言资源命名与组织结构，便于前端语言维护。

### 调整

- 将视频审核相关实现从旧命名调整为 `video_review` 体系，便于职责表达更清晰。
- 将部分通用语言与临时路径处理逻辑拆分到独立工具模块，降低主流程耦合。
- 对核心模块补充统一的作者与开源使用说明头注释。

### 文档

- 重写并对齐 `README.md`、`README.en.md` 与项目说明内容。
- 明确开源仓库中的凭证治理规则、运行前提、常用指令与版本说明入口。

## \[1.0.0] - 2026-03-03

### 首次发布

- 建立多 Agent 视频生成主流程，覆盖剧本生成、参考图生成、分镜视频生成、审核与合成。
- 接入 BytePlus 核心能力：`TOS`、`Seed-Speech`、`Seed-2.0`、`SeeDream-5.0`、`SeeDance-2.0`。
- 提供 FastAPI + WebSocket 服务基础设施与项目状态管理能力。
- 提供多语言前端界面、实时状态推送与基本交互流程。
